{
  "name": "portfolio-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "sass src/scss:dist",
    "watch": "sass --watch src/scss:dist"
  },
  "dependencies": {
    "sass": "^1.92.1"
  }
}
